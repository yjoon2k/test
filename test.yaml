apiVersion: v1
kind: Endpoints
metadata:
  name: aoldatests-external
  namespace: heimdall
subsets:
- addresses:
  - ip: 10.0.0.2
  ports:
  - port: 8080
  —
apiVersion: v1
kind: Service
metadata:
  name: aoldatests-external
  namespace: heimdall
spec:
  type: ClusterIP
  ports:
  - port: 8080
    port: 8090
  —
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aoldatests-ingress
  namespace: heimdall
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "10G"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
  - hosts:
    - {INPUT_SUB_DOMAIN}.aoldacloud.com
    secretName: wildcard-overcloud-prod-cert
  rules:
  - host: aoldatests.aoldacloud.com
    http:
      paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: aoldatests-external
                port:
                  number: 8080
