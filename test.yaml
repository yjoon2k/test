apiVersion: v1
kind: Endpoints
metadata:
  name: aoldatest-external
  namespace: heimdall
subsets:
- addresses:
  - ip: 10.0.0.1
  ports:
  - port: 8080
  —
apiVersion: v1
kind: Service
metadata:
  name: aoldatest-external
  namespace: heimdall
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: {INPUT_PORT}
  —
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aoldatest-ingress
  namespace: heimdall
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "10G"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
  - hosts:
    - {INPUT_SUB_DOMAIN}.aoldacloud.com
    secretName: wildcard-overcloud-prod-cert
  rules:
  - host: aoldatest.aoldacloud.com
    http:
      paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: aoldatest-external
                port:
                  number: 8080
